// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["www.w3schools.com"], // ✅ allow external image domain
  },
};

module.exports = nextConfig;
